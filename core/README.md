# pleasedont-die
> python

## Setup

``` bash
sudo apt-get install --no-install-recommends bluetooth bluez blueman
sudo pip install pybluez
sudo apt-get install python-serial

sudo pip install Flask
sudo pip install -U flask-cors

# TODO pairing with bt slave : sudo bluez-simple-agent hci# xx:xx:xx:xx:xx:xx
# sudo bluez-simple-agent hci# 00:14:03:05:8F:21
# sudo bluez-simple-agent hci# 98:D3:31:70:68:51
```